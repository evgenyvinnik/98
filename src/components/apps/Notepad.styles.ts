import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  textarea: {
    width: '100%',
    height: '100%',
    borderWidth: 0,
    borderStyle: 'none',
    resize: 'none',
    fontFamily: 'monospace',
    fontSize: '14px',
  },
});
